#include <bits/stdc++.h>

using namespace std;

int main() {
  int t = 1;
  printf("%d\n", t);
  while (t--) {
    int n = 1e5, p = n;
    printf("%d %d\n", n, p);
    for (int i = 0; i < n; ++i)
      printf("1 ");
    printf("\n");
  }
  return 0;
}