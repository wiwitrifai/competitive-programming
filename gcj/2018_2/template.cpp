#include <bits/stdc++.h>

using namespace std;

const int N = 505;

void solve(int tc) {
  printf("Case #%d: ", tc);
}

int main() {
  int tt;
  scanf("%d", &tt);
  for (int tc = 1; tc <= tt; ++tc) {
    solve(tc);
  }
  return 0;
}