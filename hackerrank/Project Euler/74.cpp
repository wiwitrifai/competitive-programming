#include <bits/stdc++.h>

using namespace std;

int main() {
  int d, a;
  scanf("%d%d", &a, &d);
  long long ans = 0;
  for(int i = 1; i<d; i++) {
    int r = min(i*a + i - 1, d);
    int l = a*i+1
  }

  return 0;
}